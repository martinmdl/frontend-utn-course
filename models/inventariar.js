/* const {Schema, model} = require('mongoose');

invSchema = new Schema({
    img: {
        default: 'http://image.png'
    },
    prod: {
        type: String,
        unique: true,
        require: true
    },
    desc: {
        type: String,
        require: true
    },
    precio: {
        type: Number,
        require: true
    }
})

module.exports = model('Producto', invSchema); */